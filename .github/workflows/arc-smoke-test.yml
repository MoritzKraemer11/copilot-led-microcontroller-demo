name: ARC Smoke Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: arc-runner-set   # <-- GENAU der Name aus Schritt 1
    steps:
      - name: Hello from ARC
        run: |
          echo "Runner hostname:"
          hostname
          echo "Done."
