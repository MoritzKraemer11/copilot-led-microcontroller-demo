name: ARC Smoke Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Show runner info
        run: |
          echo "Runner name: $RUNNER_NAME"
          uname -a
